- name : variables from file
  hosts: web
  become: yes
  vars_files:
    - vars.yaml
  tasks:
    - name: print variable
      ansible.builtin.debug:
        msg: " Printing Statement for {{ nam }}: {{ myvar }}"